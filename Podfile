# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'DragonBallRemote' do
    platform :ios, '10.0'
    use_frameworks!

  # Pods for DragonBallRemote
pod "CircleSlider"
 pod 'Alamofire', '~> 4.0'

  target 'DragonBallRemoteTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'DragonBallRemoteUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
